server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kubeila?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    kubeila.employee.dao.mapper: debug